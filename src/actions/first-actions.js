import { getActionType } from "../helpers";
import { api} from "../api"
import {
    GET,
    LIST,
    START,
    SUCCESS,
    FAIL
} from "../constants";

export function getSomething() {
    return async function(dispatch) {

    };
}

